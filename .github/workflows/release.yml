name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact_name: awareness
            asset_name: awareness-linux-x86_64
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact_name: awareness
            asset_name: awareness-macos-x86_64
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact_name: awareness
            asset_name: awareness-macos-aarch64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: awareness.exe
            asset_name: awareness-windows-x86_64.exe

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Linux dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libpcap-dev

      - name: Install Windows dependencies
        if: matrix.os == 'windows-latest'
        shell: powershell
        run: |
          # Download and install Npcap SDK for building
          Invoke-WebRequest -Uri "https://npcap.com/dist/npcap-sdk-1.13.zip" -OutFile "npcap-sdk.zip"
          Expand-Archive -Path "npcap-sdk.zip" -DestinationPath "C:\npcap-sdk-temp"

          # Debug: Show what was extracted
          Write-Host "Contents of extraction:"
          Get-ChildItem "C:\npcap-sdk-temp" -Recurse -Depth 2

          # Find and move the actual SDK folder
          $sdkFolder = Get-ChildItem "C:\npcap-sdk-temp" -Directory | Select-Object -First 1
          Move-Item -Path $sdkFolder.FullName -Destination "C:\npcap-sdk"

          # Verify Packet.lib exists
          Write-Host "Contents of Lib\x64:"
          Get-ChildItem "C:\npcap-sdk\Lib\x64"

          # Set LIB environment variable for linker
          Add-Content -Path $env:GITHUB_ENV -Value "LIB=C:\npcap-sdk\Lib\x64"

      - name: Build release binary
        run: |
          cargo build --release --target ${{ matrix.target }}

      - name: Strip binary (Linux and macOS)
        if: matrix.os != 'windows-latest'
        run: |
          strip target/${{ matrix.target }}/release/${{ matrix.artifact_name }}

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}
          path: target/${{ matrix.target }}/release/${{ matrix.artifact_name }}

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create release archives
        run: |
          cd artifacts
          for dir in */; do
            name=${dir%/}
            if [[ $name == *windows* ]]; then
              cd "$dir"
              zip "../${name}.zip" *
              cd ..
            else
              cd "$dir"
              tar czf "../${name}.tar.gz" *
              cd ..
            fi
          done

      - name: Generate checksums
        run: |
          cd artifacts
          sha256sum *.tar.gz *.zip > SHA256SUMS

      - name: Extract release notes
        id: extract_notes
        run: |
          # Extract version from tag
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          draft: false
          prerelease: false
          generate_release_notes: true
          files: |
            artifacts/*.tar.gz
            artifacts/*.zip
            artifacts/SHA256SUMS
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  update-readme:
    name: Update README with latest release
    needs: release
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Update README
        run: |
          VERSION=${GITHUB_REF#refs/tags/}

          # Create installation section
          cat > installation.md << 'EOF'
          ## Installation

          ### Pre-built Binaries

          Download the latest release for your platform:

          - **macOS (Apple Silicon)**: [awareness-macos-aarch64.tar.gz](https://github.com/${{ github.repository }}/releases/latest/download/awareness-macos-aarch64.tar.gz)
          - **macOS (Intel)**: [awareness-macos-x86_64.tar.gz](https://github.com/${{ github.repository }}/releases/latest/download/awareness-macos-x86_64.tar.gz)
          - **Linux**: [awareness-linux-x86_64.tar.gz](https://github.com/${{ github.repository }}/releases/latest/download/awareness-linux-x86_64.tar.gz)
          - **Windows**: [awareness-windows-x86_64.zip](https://github.com/${{ github.repository }}/releases/latest/download/awareness-windows-x86_64.zip)

          #### macOS/Linux Installation

          ```bash
          # Download and extract (adjust filename for your platform)
          tar -xzf awareness-macos-aarch64.tar.gz

          # Make executable
          chmod +x awareness

          # Move to PATH (optional)
          sudo mv awareness /usr/local/bin/

          # Run with sudo (required for packet capture)
          sudo awareness
          ```

          #### Windows Installation

          1. Download and extract the ZIP file
          2. Install [Npcap](https://npcap.com/#download) (required for packet capture)
          3. Run as Administrator (required for packet capture)

          ### Build from Source

          EOF

          # Insert after the Features section
          sed -i '/## Features/r installation.md' README.md
          rm installation.md

      - name: Commit README changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git diff --staged --quiet || git commit -m "docs: update README with release ${GITHUB_REF#refs/tags/}"
          git push origin HEAD:main
